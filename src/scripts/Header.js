export class Header {
  constructor(userBlockContainer) {
    this.userBlockContainer = userBlockContainer;
    this.userBlockContainer = userBlockContainer;
  }
  render = (userData) => {
    console.log(userData)
  };
}